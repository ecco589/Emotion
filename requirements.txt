# 机器人情感交互系统依赖包

# 基础依赖（必需）
tensorflow>=2.0.0
keras>=2.0.0
numpy>=1.19.0
opencv-python>=4.5.0
scipy>=1.5.0
pillow>=8.0.0
pandas>=1.3.0
matplotlib>=3.3.0
h5py>=3.0.0
requests>=2.25.0

# 语音识别相关（可选，用于ASR功能）
baidu-aip>=4.0.0
pyaudio>=0.2.11
keyboard>=0.13.5

# 注意：
# 1. pyaudio安装可能需要系统音频库支持
#    macOS: brew install portaudio
#    Ubuntu: sudo apt-get install portaudio19-dev
#    Windows: 通常可以直接pip install
# 
# 2. keyboard库在Linux/macOS上可能需要sudo权限
#    在macOS上可能需要允许辅助功能权限

