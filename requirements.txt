# 核心依赖（兼容 Python 3.12）
# 注意：这些版本是经过测试的兼容组合
opencv-python==4.10.0.84  # 兼容 numpy<2.0 和 TensorFlow
numpy>=1.26.0,<2.0.0  # TensorFlow 2.16+ 要求 numpy<2.0
tensorflow==2.16.1  # Python 3.12 兼容版本（自带 Keras）
scipy>=1.11.0,<1.14.0  # 兼容 numpy<2.0 和 Python 3.12
pandas>=2.0.0,<2.2.0  # 兼容 numpy<2.0 和 Python 3.12
matplotlib>=3.7.0,<3.9.0  # 数据可视化（utils中使用）
h5py>=3.9.0,<3.11.0  # HDF5文件处理（模型文件）
pillow>=8.0.0
requests>=2.25.0

# 音频处理（可选）
pyaudio>=0.2.11

# 键盘监听（可选）
pynput>=1.7.0

# 语音识别（可选，使用百度SDK时）
baidu-aip>=4.0.0
